# 更新日志

本项目的所有重要变更都记录在本文件中。

本文件格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 规范，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/) 规范。

## [未发布]

### 计划中

- 云同步（WebDAV / 坚果云）
- 插件系统与开放 API
- 多人协作编辑
- ePub / LaTeX 导出完善
- 拼写检查增强

> 详细规划见 [README.md 路线图](./README.md#-路线图)。

---

## [1.0.0] - 2025-01-01

首个正式版本。定位为**开源免费、所见即所得（Typora 类）、跨平台、轻量高性能**的
Markdown 编辑器，聚焦「P0 全部 + P1 主要项」，形成可安装、可日常使用的完整版本。

### 新增

#### 编辑器内核与实时渲染

- 基于 CodeMirror 6 构建编辑器内核，实现 Typora 式实时渲染：光标所在行显示 Markdown 源码，其余行直接显示排版结果
- 一键切换纯源码模式（`Ctrl/Cmd + /`），菜单、工具栏与状态栏均可切换
- 专注模式（`F8`）：隐藏侧边栏与状态栏，非当前段落按可配置强度淡化
- 打字机模式（`F9`）：当前编辑行始终垂直居中
- 全屏模式（`F11`）与无干扰写作体验
- 高亮当前行、显示不可见字符、自动换行、可配置 Tab 缩进宽度（1–8，默认 4）
- 成对符号自动配对与选中文字输入符号自动包裹
- 列表智能续写：回车自动续接列表项、空项回车退出列表、表格 Tab 跳格并自动补行
- 完整撤销重做栈、上移/下移当前行、复制当前行、删除当前行
- 查找与替换：支持正则表达式、区分大小写、全字匹配，支持逐个替换与全部替换，实时显示匹配计数
- 状态栏实时统计：字数、词数、行数、阅读时长、光标行列、选中字符数

#### Markdown 语法支持

- 基础语法：标题 H1–H6（含 Setext 形式）、粗体、斜体、粗斜体、删除线、行内代码、有序与无序列表（`-` `*` `+` 三种符号）、任务列表、引用块（支持嵌套）、围栏式与缩进式代码块、水平分割线、链接（含自动链接与引用式链接）、图片、GFM 表格（支持对齐）、转义字符
- 扩展语法：行内公式 `$...$` 与块级公式 `$$...$$`（KaTeX）、Mermaid 流程图/时序图/甘特图/类图、脚注 `[^1]`、目录 `[TOC]`、上标 `x^2^`、下标 `H~2~O`、高亮 `==text==`、Emoji 短代码 `:smile:`、YAML Front Matter（折叠展示）、定义列表
- 代码块语法高亮基于 highlight.js，代码块右上角显示语言名
- 公式渲染失败时以醒目的错误样式回退显示，不阻塞编辑

#### 文件与工作区

- 打开单个文件、打开文件夹作为工作区
- 新建文件、新建文件夹、重命名、删除（优先送入系统回收站）、移动、在文件管理器中显示
- 文件树面板：忽略 `node_modules`、`.git` 等无关目录与系统文件，文件夹优先、Markdown 文件优先排序，单层最多 2000 个节点
- 大纲面板：从 H1–H6 生成层级大纲并支持点击跳转
- 多标签页：标签切换、关闭、拖拽排序、未保存标记
- 最近打开文件列表（持久化、去重计数、限额可配、支持清空、启动时自动清理失效记录）
- 快速打开：模糊搜索工作区内文件（`Ctrl/Cmd + P`）
- 全局搜索：跨文件全文检索，采用 FTS5 与 LIKE 双通道取并集，兼顾英文分词与中文子串匹配，命中片段高亮显示，支持重建工作区索引并显示进度
- 外部修改检测：文件被其他程序改动时给出提示，可开关
- 文件编码识别：UTF-8（含 BOM）优先，严格解码失败时回退 GBK，兼容 Windows 遗留中文文档
- 换行符 LF / CRLF 自动识别，写回时保持原文件风格
- 自动保存：可配置间隔（1–600 秒，默认 5 秒），并在失焦与关闭前保存
- 会话恢复：启动时自动恢复上次关闭时的标签页与工作区
- 未保存内容提示：关闭标签或窗口时提供「保存 / 不保存 / 取消」三选项

#### 图片处理

- 粘贴与拖拽图片自动保存到磁盘并插入 Markdown 引用
- 四种保存方式：文档同级资源子目录（默认 `assets`）、文档同级目录、指定绝对目录、仅插入网络地址不落盘
- 图片命名模板：支持 `{yyyy}` `{MM}` `{dd}` `{hhmmss}` `{rand}` `{name}` 六种占位符，自动清理 Windows 非法字符
- 基于 SHA-256 内容哈希的图片去重，相同图片复用已有文件并标识去重命中
- 文件名冲突自动追加序号，避免覆盖既有文件
- 自动解析图片宽高（支持 PNG / JPEG / GIF / BMP / WebP，零第三方依赖）
- 图片资源登记入库，便于后续统一管理

#### 主题与外观

- 6 款内置文档主题：GitHub（默认）、Newsprint（报刊）、Night（夜色）、Pixyll（简约）、Whitey（纯白）、Vue（青绿）
- 5 款代码块高亮主题：GitHub、Dracula、Monokai、One Dark、Solarized Light
- 明暗模式：浅色 / 深色 / 跟随系统
- 自定义 CSS 主题：导入 `.css` 文件到用户数据目录的 `themes/` 文件夹即生效，可删除
- 主题体系采用「结构样式 + 配色变量」两层设计，新增主题只需提供一组 CSS 变量
- 可配置正文字体、代码字体、正文字号、行高、正文页宽、全宽模式、段落间距、行号显示、状态栏显示、侧边栏宽度

#### 导入与导出

- 导出 HTML（单文件）：内联文档主题样式、代码高亮样式与排版变量；正文含公式时额外内联 KaTeX 样式并将字体以 base64 内联，离线可用；含打印分页控制与作者信息页脚
- 导出无样式 HTML（`html-plain`）：输出纯净的语义化 HTML
- 导出 PDF：基于 Electron 内置 `printToPDF`，支持 A4/A3/A5/Letter/Legal/Tabloid 纸张、可配置页边距（mm/cm/in/pt/px）、可选页眉页脚（页眉显示文档标题与软件名，页脚显示「第 N 页 / 共 M 页」）
- 导出 PNG 长图：整篇文档渲染为一张长图，倍率 1–4 可配；内置画布尺寸与像素面积保护，超长文档自动降采样
- 检测到 Pandoc 时支持导出 Word（.docx）/ LaTeX（.tex）/ ePub（epub3）/ RTF
- 导出完成后可按设置自动用系统默认程序打开产物，每次导出结果写入导出历史

#### 版本历史

- 按可配置间隔（默认 5 分钟）自动为文档生成本地快照
- 内容哈希相同则跳过，避免产生无意义记录；超出上限（默认每篇 50 条）自动裁剪最旧记录
- 支持查看历史版本内容并一键恢复，恢复前自动为当前内容留存快照

#### 快捷键系统

- 命令表集中定义全部命令及其默认快捷键，主进程与渲染进程共用同一份契约
- 完整快捷键体系，覆盖文件操作、编辑操作、格式排版、视图与模式、表格操作、帮助六大分类
- 全部快捷键可自定义：设置面板列表化展示，点击进入「按下新快捷键」捕获状态
- 冲突检测：与既有绑定冲突时给出冲突详情并阻止保存
- 合法性校验：必须包含 Ctrl/Cmd、Alt 之一或为功能键 F1–F12
- 支持单条重置与全部重置为默认
- 快捷键方案导入/导出为 JSON，导入兼容纯数组与带「快捷键」数组两种格式

#### 本地存储

- 基于 Node.js 内置 `node:sqlite`，**零原生模块**，用户无需任何编译环境
- 启用 WAL 日志模式与 NORMAL 同步级别，兼顾并发与安全
- 建立文档索引、版本快照、标签、图片登记、设置、快捷键、最近文件、搜索索引、会话状态、导出历史等表结构
- FTS5 虚拟表提供全文检索，`unicode61` 分词；FTS5 不可用时自动降级为逐文件 LIKE 搜索
- 数据库不可用时自动降级为内存数据库，保证编辑功能仍可正常使用
- 全部写操作使用预编译语句，避免 SQL 注入
- 提供数据库信息统计与压缩整理（VACUUM）能力

#### 打包与分发

- 基于 electron-builder 打包，产物输出到 `release/` 目录
- Windows：NSIS 中文图形化安装向导，**安装时可自定义安装路径**（提供「浏览」按钮，禁止安装到系统关键目录），**可勾选是否创建桌面快捷方式与开始菜单快捷方式**（默认勾选），可勾选关联 `.md` 文件，支持覆盖安装并保留用户配置，开始菜单与「应用和功能」提供卸载入口
- Linux：提供 AppImage（免安装，`chmod +x` 后双击运行）、deb（双击安装并自动创建应用菜单项）、tar.gz（绿色解压版）三种产物
- 安装包内置 Chromium 与 Node.js 运行时，**用户无需安装 Node.js、npm、Python、Rust、Visual Studio 编译器或任何依赖**
- 独立原生窗口界面：自绘标题栏、应用内菜单栏、工具栏与状态栏，运行期不启动浏览器、不打开任何浏览器标签页、不依赖已安装的 Chrome/Edge
- 完全离线可用，无网络时全部编辑与导出功能正常
- 单实例控制：第二次启动时聚焦已有窗口并把命令行中的文件路径交给它打开；支持文件关联与拖拽到应用图标打开
- 窗口状态记忆：自动保存并恢复窗口位置与尺寸，外接显示器拔除后自动回到可见屏幕

#### 开源文档

- 新增 `LICENSE`（MIT License，Copyright (c) 2025 何飞 (He Fei)）
- 新增 `README.md` 项目主文档
- 新增 `CHANGELOG.md` 更新日志
- 新增 `THIRD-PARTY-LICENSES.md` 第三方组件许可证清单
- 新增 `docs/需求规格说明书.md` 需求基线
- 新增 `docs/详细设计说明书.md` 开发者详细设计文档
- 新增 `docs/用户手册.md` 最终用户使用手册
- 新增 `docs/测试用例清单.md` 测试用例与测试环境矩阵
- 全部源码文件附带中文注释，并在文件头标注作者与联系方式

#### 安全基线

- 渲染进程启用 `contextIsolation: true`、`nodeIntegration: false`
- 预加载脚本通过 `contextBridge` 仅暴露白名单化的类型化 IPC 接口，不向渲染进程暴露任何 Node.js 原生对象
- 页面配置内容安全策略：`default-src 'self'`、`script-src 'self'`、`frame-src 'none'`、`object-src 'none'`、`base-uri 'none'`、`form-action 'none'`，仅对 `img-src` 放开 `file:` `data:` `blob:` 以显示本地与粘贴图片
- 拒绝一切权限请求（摄像头、麦克风、地理位置等），并禁止创建额外的 webview
- 所有新窗口请求一律拒绝，外部链接交由系统默认浏览器打开，阻止应用内导航到外部地址
- `shell:open-external` 仅允许 `http` / `https` / `mailto` 协议，防止执行本地程序
- 所有 IPC 路径参数校验为绝对路径且非空
- 本地优先：默认不上传任何用户数据，完全离线可用
- 删除文件优先送入系统回收站，降低误删风险
- 运行日志按天切分并最多保留 7 天，便于问题反馈同时避免无限增长

#### 构建与工程

- 基于 esbuild 的构建脚本 `scripts/build.mjs`，分别构建主进程（CommonJS）、预加载脚本（CommonJS）、渲染进程（IIFE）、Mermaid 按需包与样式表
- 渲染进程采用 IIFE 格式，规避 `file://` 协议下模块化 import 的 CORS 限制
- Mermaid 单独成包并按需注入，避免拖慢首屏
- 构建期通过 `define` 注入版本号、软件名、作者、联系方式、项目主页与构建时间，信息只需在 `package.json` 维护一处
- 提供 `build`、`watch`、`start`、`typecheck`、`test`、`pack:dir`、`dist:win`、`dist:linux`、`dist:all` 等 npm 脚本
- 构建产物自动附带 `LICENSE`、`THIRD-PARTY-LICENSES.md`、`README.md`，满足开源合规要求
- 提供全局异常捕获，主进程未捕获异常与未处理的 Promise 拒绝均写入日志
- 提供 `typecheck` 基于 TypeScript 严格模式进行类型检查

### 变更

- **快捷键冲突处理**：原始需求中 `Ctrl+0` 同时被「普通段落」与「恢复默认字号」占用。本版本按 Typora 使用习惯，将 `Ctrl/Cmd + 0` 分配给「普通段落」，「恢复默认字号」调整为 `Ctrl/Cmd + Shift + 0`，并在设置面板中明确体现
- **技术选型调整**：需求原文档建议使用 Tauri，本项目改为 Electron。原因是硬性需求「用户免配置环境 + 独立 UI + 开箱即用」通过 Electron 可直接满足，且 PDF 导出（`printToPDF`）与长图导出为内置能力，无需 Puppeteer 与额外约 200 MB 依赖；同时 Tauri 在 Linux 下依赖系统 WebKitGTK 版本，与「免配置」要求冲突
- **本地存储方案**：采用 Node.js 内置 `node:sqlite` 替代第三方 SQLite 绑定，从根本上消除原生模块编译环节，使安装包体积与安装成功率均得到保障
- **内置文档主题数量**：需求要求不少于 5 款，实际交付 6 款（额外新增 Vue 青绿主题）
- **HTML 导出样式内联**：需求中「带样式」与「不带样式」两种 HTML 导出改为同一入口下的选项（`export.withStyle`），并额外提供独立的 `html-plain` 格式
- **窗口标题栏方案**：Windows / Linux 采用完全无边框窗口并保留缩放边框（`thickFrame`），由渲染进程自绘最小化 / 最大化 / 关闭按钮；macOS 使用 `titleBarStyle: 'hiddenInset'` 保留系统交通灯
- **菜单策略**：Windows / Linux 不显示系统菜单栏（功能全部由自绘菜单栏与快捷键接管，便于用户自定义快捷键）；macOS 提供完整中文原生菜单，以保证 ⌘Q、⌘C、⌘V 等系统级快捷键可用
- macOS 平台已具备原生菜单、交通灯标题栏、Dock 激活、`open-file` 文件关联等适配，但 v1.0.0 未提供 macOS 官方安装包
- 界面语言设置项已提供「简体中文 / English」选项，v1.0.0 以简体中文为主要界面语言

### 修复
- **代码高亮引擎由 highlight.js 升级为 Shiki**（VS Code 同款 TextMate 语法）：
  原先 highlight.js 对 Python 等语言的分词粒度过粗，`a = 10` 只标出数字，
  变量、运算符、括号全都不着色，代码块看起来「关键字、变量、符号混在一起」。
  现在关键字、函数名、变量、运算符、标点、数字、字符串各有独立配色；
  并且**按作用域自行配色**而非直接套用内置主题——因为即便是 VS Code 官方主题，
  普通变量（variable.other）也常常沿用正文色，仍然区分不明显。
  Shiki 单独打包并按需加载，首帧仍由 highlight.js 渲染，引擎就绪后自动升级。
- 修复**鼠标点击定位偏一行**的问题：标题等行级装饰原先用 `margin` 制造行间距，
  行与行之间出现空隙，而 CodeMirror 的高度模型假定各行首尾相接，导致坐标换算整体下移一行
  （点第 2 行却把光标放到第 3 行）；现改用 `padding` 并为行级内边距补上 `!important`
  （CodeMirror 基础主题的 `.cm-line` 内边距特异性更高，会静默覆盖单类选择器）。
- 修复**代码高亮主题在编辑器内不生效**的问题：编辑器里的代码配色原先是样式表里写死的深色值，
  设置中的「代码块高亮主题」只在导出结果里生效；现在 5 款代码主题在编辑器内实时生效
  （背景、边框、正文色与各类 token 颜色均跟随主题）。
- 修复**行首前缀类快捷键执行后光标停在前缀之前**的问题（如 Ctrl+1 后光标停在 `#` 前），
  原因是对行首插入未显式重映射选区、CodeMirror 默认 assoc 为 -1；现已改为 assoc = 1。
- 修复**查找 / 替换浮层无法关闭**的问题：CSS 中 `display: flex` 覆盖了浏览器对 `hidden` 属性的默认处理，
  导致查找条从应用启动起就一直显示、点击关闭按钮也无效，进而抢走输入焦点、无法正常编辑文档；
  现已在全局样式中明确 `[hidden] { display: none !important; }`，并补充了回归测试。
- 修复同一原因导致的其它显隐异常：替换输入行、状态栏（关闭「显示状态栏」后）、标题栏未保存圆点等。
- 修复光标停在代码块 / 表格 / 分割线之后的下一行行首时，整块会从渲染结果退回源码的问题（块级元素改用半开区间判定）。

- 修复图片保存时相对路径在 Windows 下出现反斜杠的问题，统一使用正斜杠并补全 `./` 前缀
- 修复重命名或移动文件后文件监视失效的问题：重命名与移动成功后自动重新注册监视
- 修复重命名目标已存在时可能覆盖用户文件的问题，改为直接拒绝并提示
- 修复新建同名文件可能覆盖既有文件的问题，改为自动追加 `(1)`、`(2)` 等序号
- 修复跨磁盘分区移动文件失败的问题，自动降级为「复制 + 删除」
- 修复图片目标文件已存在且内容不同时被覆盖的问题，改为追加 `-1`、`-2` 序号
- 修复图片模板渲染出空文件名的问题，回退为基于时间戳的默认名
- 修复图片模板可能产生 Windows 非法文件名字符的问题，统一替换为 `-`
- 修复导出 HTML 时超长文档因 `data:` URL 长度限制而失败的问题，改为写入临时文件后加载
- 修复 PDF / 长图导出时字体与图片尚未加载完成即截图导致内容缺失的问题，增加资源等待与超时兜底
- 修复长文档导出长图时超出 Chromium 画布上限导致失败的问题，增加最大高度与最大像素面积保护并自动降采样
- 修复导出 PDF 页边距仅支持单一值的问题，现支持 1 值、2 值与 4 值 CSS 长度写法
- 修复带 BOM 的 UTF-8 文件打开时首字符异常的问题，读取时剥离 BOM
- 修复 CRLF 文件保存后被改写为 LF 导致整文件差异的问题，写回时保持原有换行风格
- 修复无权限、磁盘已满、文件被占用等文件系统错误只抛出英文错误码的问题，统一翻译为中文提示
- 修复窗口在拔掉外接显示器后下次启动位置落在屏幕外的问题，启动时校验坐标是否落在可见屏幕范围
- 修复窗口最大化或全屏状态下保存尺寸导致下次启动窗口占满屏幕的问题，该状态下不记录尺寸
- 修复文件树读取超大目录可能拖死界面的问题，限制单层最多 2000 个节点
- 修复全文检索中文连写无法命中的问题，增加原文表 LIKE 通道作为兜底
- 修复搜索关键词含 `%` 或 `_` 时被当作 SQL 通配符的问题，增加 LIKE 转义
- 修复搜索命中片段直接拼接文件内容可能注入 HTML 的问题，输出前统一进行 HTML 转义
- 修复数据库被关闭后仍可能被使用导致的报错问题，统一通过单例获取并做空值保护
- 修复删除文件后全文索引仍保留旧内容的问题，删除与移入回收站时同步清理索引
- 修复导入快捷键方案时自我冲突导致条目被跳过的问题，导入前先解除该命令的旧绑定
- 修复只按下修饰键（Ctrl、Alt、Shift 等）时被记录为按键组合的问题，此时不产生绑定
- 修复 `Ctrl+0` 同时绑定两个命令导致的快捷键冲突（详见「变更」章节）
- 修复设置项为数值型时用户输入越界导致界面异常的问题，写入前按定义的取值范围自动钳制
- 修复设置表存在未知键时读取设置报错的问题，未知键原样保留为字符串
- 修复日志文件无限增长的问题，按天切分并最多保留 7 天
- 修复写日志失败可能影响主流程的问题，日志模块全部路径静默容错

### 安全

- 预加载脚本仅暴露白名单 IPC，渲染进程无法直接 `require` Node 模块，也无法访问 `ipcRenderer`
- 页面启用严格内容安全策略，禁止脚本内联、禁止嵌入外部页面与插件（`frame-src 'none'`、`object-src 'none'`）
- 主进程统一拒绝摄像头、麦克风、地理位置等一切权限请求
- 禁止创建额外 webview（`will-attach-webview` 一律阻止）
- 所有外链与 `window.open` 请求一律拒绝在应用内打开，`http`/`https` 交由系统默认浏览器处理
- 阻止渲染页面导航到 `file://` 与 `devtools://` 之外的外部地址
- `shell:open-external` 增加协议白名单（仅 `http` / `https` / `mailto`），杜绝通过 Markdown 链接执行本地程序
- 所有 IPC 路径参数强制要求为绝对路径且非空，避免意外越权访问
- 全部数据库写操作使用预编译语句与参数绑定，杜绝 SQL 注入
- Markdown 中嵌入的 Mermaid 图表经消毒处理后渲染，防止恶意图表语法注入脚本
- 本地优先：默认不上传任何用户数据，不发起任何远程请求，完全离线可用
- 删除文件默认送入系统回收站，为用户提供挽回余地

---

## 版本号说明

本项目遵循语义化版本 `主版本号.次版本号.修订号`：

- **主版本号**：不兼容的重大变更
- **次版本号**：向下兼容的功能新增
- **修订号**：向下兼容的问题修复

---

*花生苗 Markdown 编辑器 · 作者 何飞 · 微信 6731663 · MIT License*
